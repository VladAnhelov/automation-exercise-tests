# Ігноруємо node_modules (залежності)
node_modules/

# Ігноруємо результати тестів Playwright
test-results/
playwright-report/

# Локальні конфігураційні файли IDE (JetBrains, VS Code)
.idea/
.vscode/

# Файли npm, які генеруються автоматично
package-lock.json
pnpm-lock.yaml
yarn.lock

# TypeScript build output (якщо є)
dist/
out/

# Файли кешу та тимчасові файли
*.log
*.tmp

# Файли MacOS
.DS_Store

# Конфігурація системи версійності
*.swp
*.swo

# Ігноруємо environment змінні (якщо є)
.env
.env.local
.env.test
.env.production

# Playwright trace files (якщо використовуєш)
trace.zip
